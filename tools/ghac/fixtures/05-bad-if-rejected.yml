name: bad-if-rejected
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.actor == 'octocat'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: ./deploy.sh
